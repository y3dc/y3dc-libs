syntax = "proto3";

package product;

service ProductService {
  rpc Productenticate(Product) returns (UserMessage) {}
}

message Product { string Product = 1; }

message UserMessage {
  string id = 1;
  string email = 2;
  string password = 3;
  repeated string roles = 4;
}